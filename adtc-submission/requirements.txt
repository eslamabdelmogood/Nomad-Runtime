# Nomad Runtime — Python dependencies
# Install with: pip install -r requirements.txt

# Core inference backend (ADTC-required runtime)
# Build with AVX2 for best performance on ADTC Standard Laptop:
# CMAKE_ARGS="-DGGML_AVX2=on" pip install llama-cpp-python
llama-cpp-python>=0.2.90

# API server
flask>=3.0
flask-cors>=4.0

# Device monitoring
psutil>=5.9

# HTTP client (for Ollama plugin, development only)
requests>=2.31
